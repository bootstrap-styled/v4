`<ModalFooter />` component example:

```js
import { Button, ModalFooter } from '$PACKAGE_NAME';

<ModalFooter>
  <Button color="primary">Do Something</Button>
  <Button color="secondary">Cancel</Button>
</ModalFooter>
```
